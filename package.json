{
  "name": "valkyrie",
  "private": true,
  "version": "1.0.0",
  "description": "Multi-Agent SaaS Creator Platform",
  "packageManager": "npm@12.0.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.4",
    "typescript": "^5.4.5",
    "vitest": "^1.6.1"
  }
}
